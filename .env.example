# TripSync API base URL (Fastify default port)
VITE_API_URL=http://localhost:3333

# Force MSW mock handlers (useful without API/DB)
VITE_USE_MSW=false

# Auto-enable MSW when health check fails at startup
VITE_MSW_FALLBACK=true
